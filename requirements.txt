# Core scraping + data
curl_cffi>=0.7        # browser-impersonating HTTP client (defeats bot wall)
beautifulsoup4>=4.12
pandas>=2.2
pgeocode>=0.5         # offline ZIP -> lat/long for the store map
python-dateutil>=2.9

# Storage (SQLite locally / Postgres in the cloud)
SQLAlchemy>=2.0
psycopg2-binary>=2.9  # Postgres driver (Supabase/Neon)

# Dashboard
streamlit>=1.38
plotly>=5.23

# Foot traffic (Dewey Data) -- installed on demand, see README.
# deweydatapy @ git+https://github.com/Dewey-Data/deweydatapy
requests
